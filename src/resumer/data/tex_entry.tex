%-------------------------------------------------------------------------------
%	$<item>.meta.title$ - $<item>.meta.name$
%-------------------------------------------------------------------------------
$if(<item>.values)$
$if(<item>.meta.topbounded)$
\cvsection{$<item>.meta.title$}
\begin{cventries}
$endif$
    $for(<item>.values)$
    $if(<item>.meta.topbounded)$
    $else$
    \newline
    $endif$
    \cventry
    {<0>}
    {<1>}
    {<2>}
    {<3>}
    {
    $if(<item>.values.items)$
    \begin{cvitems}
        $for(<item>.values.items)$
            \item{$<item>.values.items$}
        $endfor$
    \end{cvitems}
    $endif$
    }
    \linebreak
    $endfor$
$if(<item>.meta.btmbounded)$
\end{cventries}
$endif$
$endif$